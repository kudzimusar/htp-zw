name: Migration Tests

on:
  pull_request:
    paths:
      - 'scripts/migration/**'
      - 'tests/migration/**'
      - 'package.json'
      - 'package-lock.json'
      - 'ads.txt'
      - 'app-ads.txt'
      - 'docs/migration/agent-reports/AG-03_SOURCE_DATA_CAPTURE.md'
      - 'docs/migration/agent-reports/AG-04_REHEARSAL_CONTENT_MEDIA_TAXONOMY.md'
      - 'docs/migration/14_AG04_AG05_URL_CONTINUITY_CONTRACT.md'
      - '.github/workflows/migration-tests.yml'

permissions:
  contents: read

jobs:
  migration-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Run migration tests
        run: npm run test:migration
